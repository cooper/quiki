@list: list {
    one;
    two;
    three;
};

for [@list] {
    Item at index [@index]: [@value]
}

Item at index 0: [@list.0]

@map: {
    Key 1: Value 1;
    Key 2: Value 2;
    Key 3: Value 3;
};

for [@map] {
    Pair key = [@key]; value = [@value]
}

for [@nonexist] {
    test
}

for [@nonexist as] {
}

for {
}
