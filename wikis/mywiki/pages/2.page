@page.title: A page in a category;

@category.mytopic;

Page two. It references a [[ subpage | subpages/my ]].

if [@goodvar] {
    [@goodvar] - this one shouldn't be an issue
} else {
    [@badvar] - this one produces a warning
}